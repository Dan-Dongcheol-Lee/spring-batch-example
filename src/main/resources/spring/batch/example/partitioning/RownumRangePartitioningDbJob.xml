<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns:b="http://www.springframework.org/schema/beans"
    xmlns="http://www.springframework.org/schema/batch"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.2.xsd">

    <b:bean class="spring.batch.example.configuration.BatchApplicationConfig"/>
    <b:bean class="spring.batch.example.configuration.BatchItemConfig"/>

    <job id="rownumRangePartitioningDbJob" incrementer="jobParametersIncrementer">
        <step id="rownumRangeStep">
            <partition partitioner="personRownumRangePartitioner" step="eachFileStep">
                <handler task-executor="asyncTaskExecutor" grid-size="2"/>
            </partition>
        </step>
        <listeners>
            <listener ref="elapsedTimeJobListener"/>
        </listeners>
    </job>

    <step id="eachFileStep">
        <tasklet>
            <chunk commit-interval="10"
                reader="itemReader" processor="personItemProcessor" writer="itemWriter"/>
        </tasklet>
        <listeners>
            <listener ref="personItemProcessor"/>
        </listeners>
    </step>

    <b:bean id="personRownumRangePartitioner" scope="step"
        class="spring.batch.example.partitioning.PersonRownumRangePartitioner">
        <b:constructor-arg name="jdbcTemplate" ref="jdbcTemplate"/>
        <b:constructor-arg name="query" value="select max(rn) from (select rownum as rn from person)"/>
    </b:bean>

    <b:bean id="itemReader" scope="step"
            class="org.springframework.batch.item.database.HibernateCursorItemReader">
        <b:property name="sessionFactory" ref="sessionFactory"/>
        <b:property name="queryName" value="person.findByRownumRange"/>
        <b:property name="parameterValues">
            <b:map>
                <b:entry key="maxValue" value="#{stepExecutionContext['partition_max']}"/>
                <b:entry key="minValue" value="#{stepExecutionContext['partition_min']}"/>
            </b:map>
        </b:property>
    </b:bean>

    <b:bean id="itemWriter" scope="step"
            class="org.springframework.batch.item.file.FlatFileItemWriter">
        <b:property name="resource" value="file:#{jobParameters['WRITE_FILE']}"/>
        <b:property name="appendAllowed" value="true"/>
        <b:property name="lineAggregator" ref="personLineAggregator"/>
    </b:bean>

</b:beans>
